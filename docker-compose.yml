services:
  ingestor:
    build: ./ingestor
    env_file: .env
    volumes:
      - ./data:/data
    restart: unless-stopped

  web:
    build: ./web
    env_file: .env
    volumes:
      - ./data:/data
    ports:
      - "3000:3000"
    restart: unless-stopped

